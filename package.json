{
  "name": "portfolio",
  "version": "1.0.0",
  "scripts": {
    "clean": "rm -Rf ./dist",
    "start": "npm run clean && gulp --colors && gulp watch --colors",
    "build": "npm run clean && gulp --colors --production",
    "vtex": "npm run build && gulp watch --colors --production"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/maxtrodaro/portfolio.git"
  },
  "license": "MIT",
  "homepage": "http://godoydeveloper.com.br/",
  "devDependencies": {
    "eslint": "^4.19.1",
    "eslint-config-google": "^0.9.1",
    "gulp": "^3.9.1",
    "gulp-svg-sprite": "^1.5.0",
    "taskerify": "^1.5.4",
    "underscore": "^1.9.0"
  },
  "dependencies": {
    "node-sass": "^4.11.0",
    "npm": "^6.0.1",
    "rivets-utilify": "0.0.1",
    "vtex-catalog": "^0.9.0",
    "vtex-masterdata": "^0.3.0",
    "vtex-minicart": "^0.5.0",
    "vtex-recently-viewed": "^0.0.1",
    "vtex-shelf-properties": "^0.3.0",
    "vtex-utils": "1.9.0",
    "vtex-wishlist": "^0.5.0"
  },
  "engines": {
    "node": ">= 6.0.0"
  }
}
